# 인테리어 RAW → 수평·수직만 교정 → 16-bit TIFF (EXIF 유지)
# 시스템: LibRaw (rawpy 빌드에 포함되는 경우 많음). 문제 시 OS별 LibRaw 설치 안내를 따르세요.
# EXIF 복사: brew install exiftool  (또는 https://exiftool.org/)

rawpy>=0.19.0
numpy>=1.24
opencv-python-headless>=4.8
tifffile>=2023.4.0
