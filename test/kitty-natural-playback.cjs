const {chromium}=require('playwright'),assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({headless:true,args:['--autoplay-policy=no-user-gesture-required','--use-gl=angle','--use-angle=swiftshader']});try{const p=await b.newPage({viewport:{width:390,height:844}});await p.goto(process.env.KITTY_TEST_URL||'http://127.0.0.1:8812/english-travel-3d.html');await p.evaluate(()=>{speechSynthesis.speak=()=>{throw Error('Native speech should not be called')}});const result=await p.evaluate(async()=>({engine:KittySpeech.engine,result:await KittySpeech.speak('Hello, my little friend.')}));assert.equal(result.engine,'natural-american');assert.equal(result.result,'ended');assert.equal(await p.evaluate(()=>KittySpeech.speak('Good morning!',{enabled:false})),'unavailable');console.log('PASS actual MP3 decode and playback to ended; native speech unused; mute respected');}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
